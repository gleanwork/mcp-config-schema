extensions:
  mcp_my-server:
    name: mcp_my-server
    cmd: npx
    args:
      - '-y'
      - '@example/mcp-server'
    type: stdio
    timeout: 300
    enabled: true
    bundled: null
    description: null
    env_keys: []
    envs:
      EXAMPLE_INSTANCE: your-instance
      EXAMPLE_API_TOKEN: your-api-token
